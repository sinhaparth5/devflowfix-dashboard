apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "devflowfix-chart.fullname" . }}-test-connection"
  labels:
    {{- include "devflowfix-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "devflowfix-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
